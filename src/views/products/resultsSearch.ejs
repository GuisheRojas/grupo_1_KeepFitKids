<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head') %>
        <title>Busqueda: <%= search %></title>
    </head>
<body>    
    <%- include('../partials/header') %> 
    <section class="banner">
        <img src="/img/banner.png" alt="Banner">
    </section>
    
    <main>

        <section class="results">
            <div id="titulares">
                <h4 class="titulares">Resultados de la busqueda: <%= search %></h4>
            </div>
            <% if(resultsSearch.length > 0) {%>
                <% for(let i=0; i < resultsSearch.length; i++){ %>%>
                        <article class="article">
                            <a href="/products/detailproduct/<%= resultsSearch[i].id %>">
                                <% for (let j = 0; j < products_images.length; j++) { %>
                                    <% if (resultsSearch[i].id == products_images[j].dataValues.id_product) { %>
                                        <img src=/img/products/<%= products_images[j].dataValues.name %> alt=<%= resultsSearch[i].name %> class="product_img">
                                    <% } %>
                                <% } %>
                                <p class="price">$<%=resultsSearch[i].price %></p>
                                <h4 class="description"><%=resultsSearch[i].name%></h4>
                            </a>
                        </article>
                <% } %>
            <% } else {%>
                <p>No se encontraron coincidencias con ningún producto</p>
            <% } %>
        </section>        
    </main>

    <%- include('../partials/footer') %>

</body>
</html>